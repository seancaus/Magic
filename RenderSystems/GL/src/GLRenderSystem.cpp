//
// Created by huxf on 2017/10/17.
//

#include <GLwindow.h>
#include "GLRenderSystem.h"

using namespace std;

namespace Magic {
    std::shared_ptr<RenderWindow>
    GLRenderSystem::createRenderWindow(const std::string &title, const unsigned int width, const unsigned int height,
                                       bool fullScreen) {
//        auto window = make_shared<GLWindow>("",width,height,fullScreen);
//        GLWindow w(title,width,height,fullScreen);
//        return window;
        return nullptr;
    }
}
